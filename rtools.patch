diff --git a/Makefile b/Makefile
index 9e16e73f6a..c4053ee589 100644
--- a/Makefile
+++ b/Makefile
@@ -276,6 +276,11 @@ clean:
 clean-python:
 	tools/pythonpkg/clean.sh
 
+ifeq ($(DUCKDB_PLATFORM_RTOOLS),1)
+	CMAKE_VARS:=${CMAKE_VARS} -DDUCKDB_PLATFORM_RTOOLS=1
+	echo "CIAO"
+endif
+
 debug: ${EXTENSION_CONFIG_STEP}
 	mkdir -p ./build/debug && \
 	cd build/debug && \
